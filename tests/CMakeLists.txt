# SPDX-License-Identifier: GPL-3.0-or-later

add_executable(test_foundation test_foundation.c)
target_link_libraries(test_foundation PRIVATE copal::copal)
copal_set_warnings(test_foundation)
if(COPAL_ENABLE_SANITIZERS)
    copal_enable_sanitizers(test_foundation)
endif()
add_test(NAME foundation COMMAND test_foundation)

add_executable(test_gui test_gui.c)
target_link_libraries(test_gui PRIVATE copal::copal)
target_include_directories(test_gui PRIVATE ${CMAKE_SOURCE_DIR}/src)
copal_set_warnings(test_gui)
if(COPAL_ENABLE_SANITIZERS)
    copal_enable_sanitizers(test_gui)
endif()
add_test(NAME gui COMMAND test_gui)
set_tests_properties(gui PROPERTIES SKIP_RETURN_CODE 77)

add_executable(test_textbox test_textbox.c)
target_link_libraries(test_textbox PRIVATE copal::copal)
target_include_directories(test_textbox PRIVATE ${CMAKE_SOURCE_DIR}/src)
copal_set_warnings(test_textbox)
if(COPAL_ENABLE_SANITIZERS)
    copal_enable_sanitizers(test_textbox)
endif()
add_test(NAME textbox COMMAND test_textbox)

add_executable(test_scrollview test_scrollview.c)
target_link_libraries(test_scrollview PRIVATE copal::copal)
target_include_directories(test_scrollview PRIVATE ${CMAKE_SOURCE_DIR}/src)
copal_set_warnings(test_scrollview)
if(COPAL_ENABLE_SANITIZERS)
    copal_enable_sanitizers(test_scrollview)
endif()
add_test(NAME scrollview COMMAND test_scrollview)
set_tests_properties(scrollview PROPERTIES SKIP_RETURN_CODE 77)

add_executable(test_widgets test_widgets.c)
target_link_libraries(test_widgets PRIVATE copal::copal)
target_include_directories(test_widgets PRIVATE ${CMAKE_SOURCE_DIR}/src)
copal_set_warnings(test_widgets)
if(COPAL_ENABLE_SANITIZERS)
    copal_enable_sanitizers(test_widgets)
endif()
add_test(NAME widgets COMMAND test_widgets)

add_executable(test_theme test_theme.c)
target_link_libraries(test_theme PRIVATE copal::copal)
target_include_directories(test_theme PRIVATE ${CMAKE_SOURCE_DIR}/src)
copal_set_warnings(test_theme)
if(COPAL_ENABLE_SANITIZERS)
    copal_enable_sanitizers(test_theme)
endif()
add_test(NAME theme COMMAND test_theme)

add_executable(test_popup test_popup.c)
target_link_libraries(test_popup PRIVATE copal::copal)
target_include_directories(test_popup PRIVATE ${CMAKE_SOURCE_DIR}/src)
copal_set_warnings(test_popup)
if(COPAL_ENABLE_SANITIZERS)
    copal_enable_sanitizers(test_popup)
endif()
add_test(NAME popup COMMAND test_popup)

add_executable(test_combobox test_combobox.c)
target_link_libraries(test_combobox PRIVATE copal::copal)
target_include_directories(test_combobox PRIVATE ${CMAKE_SOURCE_DIR}/src)
copal_set_warnings(test_combobox)
if(COPAL_ENABLE_SANITIZERS)
    copal_enable_sanitizers(test_combobox)
endif()
add_test(NAME combobox COMMAND test_combobox)

